---
description: Build, run, and debug a UTM change against a throwaway test VM (also dependency rebuilds via scripts/fixup.sh). Run before /utm-review.
argument-hint: [what to verify]
allowed-tools: Bash, Read, Grep, Glob, Edit, Write, Skill, Task
---

Read and follow `.agents/skills/utm-test/SKILL.md`. Use your scratchpad
directory as `$SCRATCH`, and Read every screenshot you capture: actually look at
the image. Run long builds in the background.

What to verify in this run (if any): $ARGUMENTS
